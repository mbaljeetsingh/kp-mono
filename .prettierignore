packages/crawler/out/
pnpm-lock.yaml

# Written by the Supabase CLI while the local stack runs. Untracked, and not
# ours to format.
supabase/.temp/

# Generated by the shadcn CLI — left in upstream formatting so a re-add
# produces a clean diff rather than a whole-file reformat.
packages/ui/src/components/ui/*
